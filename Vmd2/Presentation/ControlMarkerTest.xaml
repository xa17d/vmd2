<UserControl x:Class="Vmd2.Presentation.ControlMarkerTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vmd2.Presentation"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding MarkerX}" Width="70"></TextBox>
            <TextBlock>x</TextBlock>
            <TextBox Text="{Binding MarkerY}" Width="70"></TextBox>
            <Button Click="buttonFromMarker_Click">From Marker</Button>
        </StackPanel>
    </Grid>
</UserControl>
